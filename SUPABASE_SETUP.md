# Supabase — Stage 134

شغّل الملف التالي مرة واحدة في SQL Editor قبل اختبار تسجيل السائق المبسط:

`supabase/stage_134_driver_fast_signup_approval_switch.sql`

المفروض تظهر في نتيجة النهاية:
- `driver_signup_open_now = true`
- `driver_fast_signup_ready = true`

بعدها يمكن التحكم بموافقة السائقين من لوحة الإدارة Stage 14 بدون تعديل الكود.
